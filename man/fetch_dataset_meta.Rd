% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_datasets.R
\name{fetch_dataset_meta}
\alias{fetch_dataset_meta}
\title{Fetch nrows/ncols for a vector of dataset ids concurrently via curl's
multi-handle pool.}
\usage{
fetch_dataset_meta(ids, max_concurrent = 10, timeout = 30)
}
\arguments{
\item{ids}{Character vector of dataset ids, e.g. "google/dataset-one".}

\item{max_concurrent}{Integer. Max simultaneous connections in the pool.}

\item{timeout}{Numeric. Per-request timeout in seconds.}
}
\value{
A data.table with columns: id, nrows, ncols.
}
\description{
A single call to \verb{/size} per dataset is enough. First the \code{configs[]}
array is checked: if the dataset does not have exactly one config,
nrows/ncols are both NA. If it does have exactly one config, \code{nrows} is
computed by summing \code{num_rows} across all of that config's entries in
the \code{splits[]} array (rather than trusting the config-level aggregate
directly), and \code{ncols} is read from \code{num_columns} on one of those
splits (column count doesn't vary across splits of the same config).
}
\details{
If a dataset has zero or more than one config, both nrows and ncols are
set to NA -- there's no single unambiguous row/column count to report
in that case.
}
