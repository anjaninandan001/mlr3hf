% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HFData.R
\name{as_task.HFData}
\alias{as_task.HFData}
\title{Coerce an HFData Object to an mlr3 Task}
\usage{
\method{as_task}{HFData}(x, target_names = NULL, task_type = c("auto", "classif", "regr"), ...)
}
\arguments{
\item{x}{(\code{HFData})\cr The \code{HFData} object to convert.}

\item{target_names}{(\code{character(1)})\cr Name of the target column. If \code{NULL}, the \code{target} stored in \code{x} is used. Multiple targets are not supported.}

\item{task_type}{(\code{character(1)})\cr One of \code{"auto"}, \code{"classif"}, or \code{"regr"}. Defaults to \code{"auto"}, in which case the type is inferred from the target column.}

\item{...}{Additional arguments (currently unused).}
}
\value{
An \link[mlr3:TaskClassif]{mlr3::TaskClassif} or \link[mlr3:TaskRegr]{mlr3::TaskRegr} object.
}
\description{
Converts an \link{HFData} object into an \code{mlr3} \link[mlr3:Task]{Task}, either a
\link[mlr3:TaskClassif]{TaskClassif} or a \link[mlr3:TaskRegr]{TaskRegr}. The
target column and task type can be taken from the \code{HFData} object or
overridden explicitly.

When \code{task_type = "auto"}, the task type is inferred from the class of
the target column: factor and logical columns become classification
tasks, numeric and integer columns become regression tasks, and
character columns become classification tasks only if they have at most
50 unique values or a unique-value ratio below 10\%; otherwise an error is
raised asking for an explicit \code{task_type}.
}
\examples{
\dontrun{
hf <- HFData$new("user/dataset", config = "default", target = "label")
task <- as_task(hf)

# Override the target and task type explicitly
task2 <- as_task(hf, target_names = "label", task_type = "classif")
}
}
